services:
  - type: web
    name: 03-vsmi-web
    env: static
    buildCommand: echo "No build needed"
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
